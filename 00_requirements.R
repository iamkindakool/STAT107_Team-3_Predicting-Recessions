 ## Install
library(fredr)         ## Load package to environment.
## Install
library(dplyr)            ## Load package to environment.
## Install
library("ggplot2")            ## Load package to environment.
## Install
library(quantmod)            ## Load package to environment.

