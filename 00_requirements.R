

library(fredr)
install.packages("fredr", dependencies = TRUE)
installed.packages()["fredr",]
install.packages("fredr")
##We install this package for knowing the US recession.

library(dplyr)
##Use it to code for data flames.

library("ggplot2")
install.packages("ggplot2")
##use for visual data.

install.packages("quantmod")
library(quantmod)
##This data is use for answer the Market stock and trade value.

